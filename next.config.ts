import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['i.ibb.co', 'source.unsplash.com'], // Add i.ibb.co (or ibb.co, depending on the direct image host)
  },
};

export default nextConfig;
